def find(root_idx, tree):
    # this method iterates through all children and call recursive function that returns height
    # height = 1
    # for c in children:
    # height = max(height, find(children, tree)
    # return height
    pass


if __name__ == '__main__':
    n = input()
    nodes = list(map(int, input().split()))
